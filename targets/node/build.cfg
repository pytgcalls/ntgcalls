[options]
workdir = {target}
platforms = linux-x64, linux-arm64, macos-arm64, windows-x64
tools = node
publish = both

[build]
install =
    npm install
compile =
    npm run build -- --directory {root} --out {out} --cmake-path {cmake} --CDBINDING=node --CDSTATIC_BUILD={static} --CDCMAKE_TOOLCHAIN_FILE={toolchain}

[publish]
artifact =
    node support/pack-platform.mjs --binary build/Release/ntgcalls.node --platform {platform} --arch {arch} --version {version}
    npm publish packages/{platform}-{arch} --access public
final =
    node support/stamp.mjs --version {version}
    npm publish --access public
